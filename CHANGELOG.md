# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- Added `.sqlfluff` configuration for SQL linting.
- Added `.pre-commit-config.yaml` for local code quality checks.
- Added GitHub Actions CI workflow for linting and project parsing.
- Added `packages.yml` documentation in README if missing.

### Changed
- Updated `pyproject.toml` to include development dependencies (`sqlfluff`, `pre-commit`).
- Improved project structure and configuration standards.

